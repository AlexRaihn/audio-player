# Audio Player
# Vue 3 + TypeScript + Vite

This repository provides an audio player component where you only need to pass the current audio index and an array of songs with the following format:

id: number;
title: string;
artist: string;
url: string;

Here's the translation:

To use it, you need to install Tailwind CSS, or define the styles for it yourself.
